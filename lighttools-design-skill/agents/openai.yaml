interface:
  display_name: "LightTools Design"
  short_description: "Reproduce optical papers and build validated LightTools models."
  default_prompt: "Use the LightTools design skill to reproduce this optical paper or build a traceable LightTools model, including assumptions, automation steps, validation outputs, and an audit trail."
